const Countries = ({ countrySearch, setSelectedCountry }) => <ul>{countrySearch.map(country =><li key = {country.cca3}> {country.name.common} <button onClick = {() => setSelectedCountry(country)}>Show</button></li>)}</ul>
export default Countries